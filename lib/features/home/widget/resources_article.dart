import 'package:flutter/material.dart';

class ResourcesArticle extends StatelessWidget {
  const ResourcesArticle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 250);
  }
}
